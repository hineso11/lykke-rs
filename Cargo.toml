[package]
name = "lykke-rs"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
tonic = { version = "0.6.2" }
prost = "0.9.0"
prost-types = "0.9.0"

[build-dependencies]
tonic-build = "0.5.1"